# Copy this file to `.env` and fill in real values.
# The real .env is gitignored — never commit it.
# These must also be set in your Vercel project's Environment Variables.

# OpenAI
OPENAI_API_KEY=
OPENAI_ORG=
OPENAI_PROJECT=

# DeepSeek (used ~1% of the time as a fallback model)
DEEPSEEK_API_KEY=

# Firebase Realtime Database
FIREBASE_API_KEY=
